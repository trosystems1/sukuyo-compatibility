# architecture.md — sukuyo-compatibility

このファイルは「なぜこの構成にしたか」を記録する。`index.json`が地図（What/Where）なら、こちらは理由（Why）。
コード変更や設計変更の可能性がある作業の前には、エージェントは必ずこのファイルを先に読むこと。

## プロジェクトの目的
竹本光晴堂（kosei-do.co.jp）が提供する宿曜占星術の鑑定サービスのWeb版補助ツール。
本命宿・月のルネーション・対人/対法人の相性診断を、生年月日の入力だけで算出して表示する。

## 現在の構成
- アプリ本体は`index.html`中心の静的HTML/CSS/JavaScript構成。
- 補助ページとして`senseiban.html`（占星盤）と`sukuyo-about.html`（宿曜占星術の説明）を持つ。
- Vercelでは`vercel.json`の`framework: null`により静的サイトとして配信する。
- iOS/Capacitor向けには`npm run sync:www`で`index.html`、`senseiban.html`、`sukuyo-about.html`、`assets/`を`www/`へ同期する。
- Next.js/Reactのビルドパイプラインは使わない。

## なぜ静的HTMLファイル構成なのか
- ビルドステップを持たず、HTML/CSS/JSを直接編集してVercelにpushするだけで反映される運用を優先している。
- フレームワーク（React/Next.js等）を使わず、旧暦変換も外部CDN依存なし（`index.html`内の朔テーブル）。
- 理由（推定）：占いツールという性質上、機能追加よりも「文言・スコアの微調整」「UIの見た目調整」の頻度が高く、
  ビルド工程を挟まない方が竹本先生側との細かいやり取りやデプロイサイクルが速い。
- ※この判断の背景（意図的なシンプル化か、単に最初の規模感で始まってそのままなのか）は未確認。次回確認したい。

## 宿曜計算ロジックの設計
- 新暦→旧暦変換は JST 基準の朔・中気から構築した朔テーブル（1899–2100年、`KYUREKI_SAKU`）を使う。
  中国農暦ライブラリ（UTC+8）は使わない。朔が JST 0時台に入る月で1日ずれ、宿も1つずれるため
  （例: 1976/12/8 は日本旧暦10/17→井宿、中国農暦10/18→鬼宿）。
- 27宿の割り当てロジックは「旧暦の各月1日の宿が固定（1月=室, 2月=奎, …12月=虚）、月内は+1で進む」という規則ベース。
  → `kyurekiLookup` が旧暦日と宿 index を返し、`calcShukuIndex` がその宿 index を返す。
- 正しさの担保は `npm test` で行う。README記載の検証済み確定値（1962/3/22→底宿、1975/8/11→亢宿、1973/1/1→箕宿、1976/12/8→井宿）と、公開こよみ用27宿データの網羅性を検証する。
  → ロジックや公開こよみデータを変更した場合は、必ずテストを通すこと。新しい確定ケースが得られたら追加していく方針。

## なぜBasic認証をmiddleware.jsで実装しているか
- 検索エンジンに非公開（vercel.jsonでの非表示設定とあわせて二重に防御）にしたいクローズドな提供物のため。
- 例外として、公開こよみの query route（`?daily`）だけはログインなしで閲覧できる。URLフラグメントはmiddlewareに届かないため、本番で公開共有する場合は `index.html?daily=today` や `index.html?daily=昴宿` を使う。
- Vercelの環境変数（`BASIC_AUTH_USER` / `BASIC_AUTH_PASS`）で本番の認証情報を上書きする運用。
  コード内にはローカル用のフォールバック値があるが、本番でこのフォールバックのまま運用しないこと。

## 禁止されている操作・注意点
- `SHUKU`配列やひと月の起点となる宿の対応表を変更する際は、必ずREADMEの検証済みテストケースで再検証する。
  （27宿の割り当てがズレると、サービス全体の鑑定結果が全件ズレるため、影響範囲が大きい）
- `package.json`に依存パッケージを追加する場合、ビルドステップが発生しないかを確認すること。
  （現状はNo-build運用が前提のため、ビルドが必要になる変更は設計方針との衝突になる）
- `www/`はCapacitor向けの同期結果なので、通常はルート側のHTML/アセットを更新してから`npm run sync:www`で反映する。
- middleware.jsのフォールバック認証情報を本番運用の前提にしない。

## 未確定・要確認事項
- 静的HTMLファイル構成を維持する方針が今後も続くか、機能が増えた場合に分割する基準があるか
- COMPAT_TABLE（11分類の関係性）やgetRelAdviceのアドバイス文言は、竹本先生の鑑定メソッド由来の固定ロジックなのか、
  今後チューニングが入る想定があるのか
